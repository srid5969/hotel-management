export const fileNameRegex = {
  marketSegment: /MIS B Market Segment as on (\d{2}) (\w{3}) (\d{4}).xlsx/,
  businessSource: /MIS B Business Source as on (\d{2}) (\w{3}) (\d{4}).xlsx/,
  historyAndForecast:
    /History & Forecast (\d{1,2}) (\w{3})  to (\d{1,2}) (\w{3}) (\d{4}).xlsx/,
  coverAnalysis: '',
};
